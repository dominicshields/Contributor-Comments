I want a new feature, the idea is that when the users add a comment they have the option to pull in a standard wording
from a popup list, this popup list gets its values from some new metadata called Available Templates that should be accessed, created, edited and also viewed from a new submenu of System Config.
On select of a particular wording, this is put into the comment field and the comment remains editable as normal.

Please examine the image template_wording for a number of individual template wordings to populate the template metadata with.

Please put any questions below for me to respond. Once you have the information you require please implement.

## Questions

1. I cannot extract text content from the image reliably in this environment. Please paste the full template wording list here (or provide it as text in a file such as markdown/csv).

Contact gave figures over the 'phone
Contact unavailable until ...... recontact
Contributor checking queries and will 'phone back
Contributor unable to complete form by deadline due to ........ will return by .....
Emailed/faxed duplicate form on request
Enforcement call made...
Estimated figures given, correct figures will be sent in asap
Left message with colleague/on voicemail chasing form
Nil return given over the 'phone
No inquiry form received
Requested return of overdue form

2. When a user selects a template wording, should it:
	- replace the current comment text, or
	- append to the existing comment text?
A. Never overwrite, append.

3. Should the template popup on Add Comment be available to all authenticated users, or admin users only?
A. All users

4. Should Available Templates be a single flat list, or grouped by category/type?
A. Currently a flat list